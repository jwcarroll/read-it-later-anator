# Frontend Static Artifact (Bootstrap)

Amplify Hosting currently publishes files from this directory directly.

During this infra-only bootstrap phase, this folder is treated as the deployable web artifact source.
